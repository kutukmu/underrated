import React from "react";
import Layout from "./Components/Layout";
import Home from "./Components/Home";
const App = (props) => {
  const { session, client, refetch } = props;

  return <div></div>;
};

export default App;
